import React from 'react';
import st from './slider.module.css'

const Slider = () => {
    return (
        <div className={st.slider}>
            222
        </div>
    );
};

export default Slider;