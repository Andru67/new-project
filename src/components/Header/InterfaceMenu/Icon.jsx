import React from 'react';
import st from '../headder.module.css'

const Icon = () => {
    return (
        <div className={st.menu}>
            <i className={st.icon}></i>
        </div>
    );
};

export default Icon;